apiVersion: apps/v1
kind: Deployment
metadata:
  name: favorites-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: favorites-service
  template:
    metadata:
      labels:
        app: favorites-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3003"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: pokeshop-sa
      containers:
      - name: favorites-service
        image: 704225640908.dkr.ecr.us-east-2.amazonaws.com/pokeshop-favorites-service:latest
        ports:
        - containerPort: 3003
        envFrom:
        - configMapRef:
            name: pokeshop-config
        env:
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: favorites-secrets
              key: db_host
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: favorites-secrets
              key: db_user
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: favorites-secrets
              key: db_password
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: favorites-secrets
              key: db_name
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: shared-secrets
              key: jwt_secret
        - name: KAFKA_BROKERS
          valueFrom:
            secretKeyRef:
              name: shared-secrets
              key: kafka_brokers
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 10
          periodSeconds: 5
